# Docker Infrastructure Notes

This directory hosts environment-level Docker notes and templates.

Current local runtime uses repository root docker-compose.yml for:

- Kafka + Zookeeper
- Redis
- PostgreSQL with pgvector
